# Examples repo.
Example programs for various tasks.

- `pip.c`: Simple demonstration of launching a child process that replaces itself with another program (VIM), with its parent then waiting for it to exit.
